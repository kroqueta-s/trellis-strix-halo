# Pure-python dependencies only. **No CUDA-only package appears here.**
# spconv / flash-attn / xformers / kaolin / nvdiffrast are replaced at launch
# time by the shims in runners/trellis/ (see shims.py and raster.py).
#
# Versions are the ones this was verified with on 2026-09-01.
tqdm==4.70.0
easydict==1.13
scipy==1.18.1
trimesh==5.1.0
transformers==5.16.1
safetensors==0.8.0
huggingface_hub==1.29.0
imageio==2.37.4
opencv-python-headless==5.0.0.93
einops==0.8.2
plyfile==1.1.5
rembg==2.0.81
onnxruntime==1.29.0
python-dotenv==1.2.3
utils3d @ git+https://github.com/EasternJournalist/utils3d.git@9a4eb15e4021b67b12c460c7057d642626897ec8

# **pymeshfix (AGPL-3.0) and igraph (GPL) used to be here** - the hole filling
# and the min-cut in fill_holes.py. **This repository is MIT**, and nothing it
# needs at runtime should be more restrictive, so both were replaced:
# the cut by scipy's maximum flow (tests/test_min_cut.py holds the two to the
# same cost), the hole filling by runners/trellis/close_holes.py. Measured on
# the same specimen afterwards: 518,576 faces against 525,992, one component,
# **0 boundary edges, 0 non-manifold edges, watertight**, and the enclosed
# volume within 0.26%.
xatlas==0.0.11
